version: '3.9'

services:
  {{ service_name }}:
    build: .
    container_name: {{ service_name }}
    env_file:
      - .env
    networks:
      - {{ rabbitmq_network }}

networks:
  {{ rabbitmq_network }}:
    external: true
